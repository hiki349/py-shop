# PY-SHOP

## Install Steps

- Fetch project
  ```
  git clone https://github.com/hiki349/py-shop.git
  ```

- Start Project
  ```
  make app
  ```

- Read the docs
  ```
  http://localhost:8000/api/docs
  ```
